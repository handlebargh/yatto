from_latest_tag = true
tag_prefix = "v"

[git_hooks.commit-msg]
script = """#!/bin/sh
set -e
cog verify --file $1
"""

[git_hooks.pre-commit]
script = """#!/bin/sh
set -e
just lint
just test-cache
"""

[changelog]
path = "CHANGELOG.md"
